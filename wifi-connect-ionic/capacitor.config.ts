import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ryangoce.wifi',
  appName: 'wifi-connect-ionic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
